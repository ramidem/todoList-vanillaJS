/**
 * makeTurkeySandwich
 *    Get one slice of bread.
 *    Add Turkey.
 *    Put a slice of bread on top.
 */

function makeTurkeySandwich() {
  // Get one slice of bread.
  // Add Turkey.
  // Put a slice of bread on top.
}

// Customize a function
function makeSandwichWith(filling) {
  // Get one slice of bread.
  // Add filling.
  return `${filling} sandwich`;
  // Put a slice of bread on top.
}

console.log(makeSandwichWith("egg"));
